#include<bits/stdc++.h>
using namespace std;
string fibonacci(int n)
{
    string s;
    if(n==0) return "a";
    else if(n==1) return "b";
    else return fibonacci(n-1)+fibonacci(n-2);
}
int main()
{
    int n;
    cin >> n;
    cout << fibonacci(n);
    return 0;
}
